import "../../system/preamble.prag"


[
    "compile.entry", 
    "compile.run":"true",
    "compile.opt": "0",
    "compile.debuginfo": "true"
]
let main = fun () => void {    
    print("hello, world!\n");
    for (var i = 0; i < 12; ++i) {
        debug_print("i", i);
    }
}

