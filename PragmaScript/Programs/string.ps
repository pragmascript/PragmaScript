let print = (str: string) =>
{
	print_string(str.length, str.data);
}

let vec = struct
{
	x: float32;
	y: float32;
	z: float32;
}

let add = (a: float32, b: float32) => 
{
	return a + b;
}

let print_vec = (v: vec) => 
{
	print("(");
	print_f32(v.x);
	print(", ");
	print_f32(v.y);
	print(", ");
	print_f32(v.z);
	print(")");
}

var v = vec { 1.0, 2.0, add(1.0, -2.0) };
print_vec(v);
print("\n");

let print_int32_array = (a: int32[]) => 
{
	for (var i = 0; i < a.length; ++i)
	{
		print_i32(a[i]);
		if (i < a.length - 1)
		{
			print(", ");
		}
	}
}

var x = [1, 2, 3, 4, 12];
print_int32_array(x);