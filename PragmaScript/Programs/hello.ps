// let print_string = (s: string) => 
// {
// 	var handle = GetStdHandle(-11);
// 	var ignored = WriteFile(handle, s.ptr, s.length, nullptr, nullptr);
// }

// print_string(s);

var ch = GetStdHandle(-11);
var s = "Hello World! fumm fumm._:\n"; 
WriteFile(ch, s.data, s.length, nullptr, nullptr);


var str = "FOOOOOOOOoasdoakdoks\n";
for (var i = 0; i < 12; ++i) {
	WriteFile(ch, str.data, str.length, nullptr, nullptr);
}







